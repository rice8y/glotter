[package]
name = "glotter"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Eito Yoneyama"]
license = "MIT AND CC-BY-SA-3.0"
description = "Detect languages from text fragments."
keywords = ["language", "detection", "fasttext", "multilingual"]
categories = ["languages", "text"]
exclude = ["/examples", "/tests". "/images"]